"
" Insert Mode Abbreviations
"

iabbr #D        #define
iabbr #I        #include
iabbr #W        #warning

iabbr teh       the
iabbr mroe      more

iabbr #f        for ( ; ; ) {<cr><cr>}
iabbr #i        if ( ) {<cr><cr>}
iabbr #w        while ( ) {<cr><cr>}
